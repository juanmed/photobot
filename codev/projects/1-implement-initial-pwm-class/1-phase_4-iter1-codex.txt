**Findings (Phase 4 only)**

1. **Lock does not protect `_channels` or `_timings` as required by the phase plan.**  
`update()` reads `self._channels` and appends to `self._timings` outside the lock, while `enable_channel()` / `disable_channel()` mutate `self._channels[channel_id].enabled` under the lock. This violates the Phase 4 deliverable (“Lock protecting `_pending`, `_channels`, `_timings` from concurrent mutation”) and introduces a race between `update()` and concurrent enable/disable calls.  
File refs: `sw/pwm_controller.py` (update loop + timing append)  

2. **Plan alignment gap in `update()` snapshot strategy.**  
Phase 4 implementation details specify: snapshot and clear `_pending` under lock, then perform hardware I/O outside the lock. To meet the “protect `_channels` from concurrent mutation” requirement without holding the lock during I/O, `update()` should also snapshot enabled state (and optionally PWM instances) under the lock. The current implementation does not.  
File refs: `sw/pwm_controller.py` (update)

**Spec Adherence (Phase 4 scope)**  
Partially. Thread-safety is documented and lock exists, but the lock does not cover `_channels` and `_timings` mutations as required by the phase objectives.

**Code Quality**  
Readable and consistent with previous phases, but the concurrency contract stated in the plan isn’t fully implemented.

**Test Coverage (Phase 4 scope)**  
There is a Phase 4 test for lock release during I/O and a concurrency smoke test. However, there’s no test that would catch races on `_channels` or `_timings`. This leaves a gap relative to the phase deliverables.

**Error Handling**  
No new error handling issues specific to Phase 4. The concurrency gap could lead to inconsistent enable/disable behavior under contention.

**Plan Alignment**  
Not fully aligned: lock does not protect `_channels` and `_timings` as explicitly required; update does not snapshot enabled state under lock.

---

VERDICT: REQUEST_CHANGES  
SUMMARY: Phase 4 thread-safety is incomplete: `_channels` and `_timings` are accessed/mutated outside the lock, violating the plan’s concurrency requirements.  
CONFIDENCE: HIGH  

---

KEY_ISSUES:  
- `update()` reads `_channels` and appends to `_timings` outside the lock, contradicting the Phase 4 deliverable to protect `_channels` and `_timings` from concurrent mutation.  
- `update()` does not snapshot enabled state under lock, leaving a race with `enable_channel()` / `disable_channel()` and violating the Phase 4 design intent.

**Suggested fix direction (within Phase 4 scope):**
1. Under `self._lock`, snapshot `_pending`, enabled flags, and the `HardwarePWM` instance mapping you intend to use.
2. Release the lock before any hardware I/O.
3. Append timing stats under the lock (or use a dedicated lock around `_timings`) to satisfy the deliverable.